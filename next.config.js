/** @type {import('next').NextConfig} */
const nextConfig = {
  // Explicitly tell Next.js where the app lives
  // (required when using src/ directory on some Vercel configs)
};

module.exports = nextConfig;
